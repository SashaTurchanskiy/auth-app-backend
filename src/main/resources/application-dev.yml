#development version of configuration
server:
  port: 8083

  #configurations
  #database configuration
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/auth_app_db?useUnicode=true&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: qwer1234
    driver-class-name: com.mysql.cj.jdbc.Driver

#    hikari:
#      pool-name: HikariPc
#      maximum-pool-size: 5
#      connection-timeout: 10000
#      idle-timeout: 600000
#      max-lifetime: 1800000
#      validation-timeout: 1800000
#      leak-detection-threshold: 0
#      initialization-fail-timeout: -1

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: false
      open-in-view: false
      properties:
        hibernate:
          format_sql: true

security:
  jwt:
   secret: ${JWT_SECRET:dfhgidfhgdjgjsoijfgojspoikjiotjoinjakppopoibntrbnoamsboimtroiaodrdrbdb}
   issuer: ${JWT_ISSUER:api.substring.com}
   access-ttl-seconds: ${JWT_ACCESS_TTL_SECONDS:3600}
   refresh-ttl-seconds: ${JWT_REFRESH_TTL_SECONDS:86400}
   refresh-token-cookie-name: ${JWT_REFRESH_COOKIE_NAME:refreshToken}
   cookie-secure: ${JWT_COOKIE_SECURE:true}
   cookie-http-only: ${JWT_COOKIE_HTTP_ONLY:true}
   cookie-same-site: ${JWT_COOKIE_SAME_SITE:lax}